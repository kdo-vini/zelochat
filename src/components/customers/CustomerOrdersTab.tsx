import type { CustomerDetail } from '../../services/customerApi';

export function CustomerOrdersTab({ customer }: { customer: CustomerDetail }) {
  const average = customer.orderCount ? customer.totalValue / customer.orderCount : 0;
  return <div className="p-4"><div className="mb-4 grid grid-cols-3 gap-2 text-center"><div className="rounded-lg bg-[var(--color-surface-muted)] p-2"><strong className="block text-[15px] text-[var(--color-ink)]">{customer.orderCount}</strong><span className="text-[11px] text-[var(--color-ink-faint)]">Pedidos</span></div><div className="rounded-lg bg-[var(--color-surface-muted)] p-2"><strong className="block text-[15px] text-[var(--color-ink)]">R$ {customer.totalValue.toFixed(2).replace('.', ',')}</strong><span className="text-[11px] text-[var(--color-ink-faint)]">Valor total</span></div><div className="rounded-lg bg-[var(--color-surface-muted)] p-2"><strong className="block text-[15px] text-[var(--color-ink)]">R$ {average.toFixed(2).replace('.', ',')}</strong><span className="text-[11px] text-[var(--color-ink-faint)]">Ticket médio</span></div></div>{customer.orders.length === 0 ? <p className="text-[13px] text-[var(--color-ink-muted)]">Nenhum pedido vinculado.</p> : <ul className="divide-y divide-[var(--color-line)]">{customer.orders.map((order) => <li key={order.id} className="flex items-center justify-between py-3 text-[13px]"><span className="text-[var(--color-ink-muted)]">{new Date(order.createdAt).toLocaleDateString('pt-BR')} · {order.status}</span><strong className="text-[var(--color-ink)]">R$ {order.total.toFixed(2).replace('.', ',')}</strong></li>)}</ul>}</div>;
}
